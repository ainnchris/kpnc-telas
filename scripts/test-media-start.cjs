const test=require('node:test'),assert=require('node:assert/strict'),vm=require('node:vm'),fs=require('node:fs'),path=require('node:path');
function load(){const scope={window:{},console:{info:()=>{},warn:()=>{}}};vm.runInNewContext(fs.readFileSync(path.join(__dirname,'../public/js/media-start.js'),'utf8'),scope);return scope.window.MeetMedia.startLocalMedia;}
test('pending speaker selection never blocks initial mic/camera capture',async()=>{const calls=[],room={localParticipant:{setMicrophoneEnabled:async()=>calls.push('mic'),setCameraEnabled:async()=>calls.push('camera')},switchActiveDevice:()=>{calls.push('output');return new Promise(()=>{})}};await load()(room,{mic:true,camera:true,devices:{output:'headset'}},()=>{});assert.deepEqual(calls,['mic','camera','output']);});
test('camera still starts if mic is denied; default device is not forced',async()=>{let options,notice;await load()({localParticipant:{setMicrophoneEnabled:async()=>{throw new Error('Denied')},setCameraEnabled:async(_,o)=>{options=o}}},{mic:true,camera:true,devices:{video:'default'}},m=>notice=m);assert.equal(options.deviceId,undefined);assert.match(notice,/Microfone/);});
